// Simple enum grouping available unit categories in the system.
// Categories: LENGTH, MASS, VOLUME, TEMPERATURE.
// Used to ensure conversions happen only within the same category.
public enum UnitCategory{
    LENGTH,
    MASS,
    VOLUME,
    TEMPERATURE
}
